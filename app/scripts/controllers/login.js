'use strict';

angular.module('musicApp')
	.controller('LoginCtrl', function ($routeParams, $scope, $rootScope, $location, $interval, $timeout, SongService, YoutubeAPI, SoundCloudAPI) {

		/**
		 * Run it!
		 * @return {undefined}
		 */
		var init = function() {


		}

		init();

	});
