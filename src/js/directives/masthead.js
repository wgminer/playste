app.directive('plMasthead', function ($http, $rootScope, YouTube, SoundCloud, Playlist) {
    return {
        restrict: 'A',
        link: function (scope, element, attrs) {
    
        }
    }

});